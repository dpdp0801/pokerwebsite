{
  "buildCommand": "./prisma-fix.sh && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "cleanUrls": true,
  "outputDirectory": ".next",
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ]
} 